<!DOCTYPE html>
<html>
<head>
  <title>Hello World</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Pacifico&display=swap');

    body {
      background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      font-family: 'Pacifico', cursive;
      color: white;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
      animation: bgShift 10s ease-in-out infinite alternate;
    }

    h1 {
      font-size: 4rem;
      animation: float 3s ease-in-out infinite;
    }

    p {
      font-size: 1.5rem;
      animation: fadeIn 4s ease-in-out forwards;
      opacity: 0;
      margin-top: 20px;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    @keyframes fadeIn {
      to { opacity: 1; }
    }

    @keyframes bgShift {
      0% { background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%); }
      50% { background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%); }
      100% { background: linear-gradient(135deg, #fccb90 0%, #d57eeb 100%); }
    }
  </style>
</head>
<body>
  <h1>Hello World 🌟</h1>
  <p>Ruby on Rails ile ilk animasyonlu sayfam 💖</p>
</body>
</html>
